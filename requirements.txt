# Runtime dependencies (CPU-only). Kept minimal; ML deps added per milestone.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9   # multipart form uploads (/v1/enroll)

# audio ingestion (C1.0): ffmpeg (system) decodes; numpy holds PCM.
numpy>=1.26,<2.3
# ID embedder (A.2): pure-numpy fbank → ONNX model (CPU). soundfile reads wavs.
onnxruntime>=1.17
soundfile>=0.12
# voiceprint store (A.4): AES-256 at-rest encryption.
cryptography>=42.0

# --- added in later milestones ---
#   scipy>=1.11            # clustering / signal (our ONNX diarizer, E.3)

# .env loading (config.py) — local secrets like the Google OAuth client
python-dotenv>=1.0
