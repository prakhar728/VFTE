# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# venv
.venv/
.venv-diart/
venv/

# local data & secrets — never commit voiceprints/keys
data/
*.db
*.db-wal
*.db-shm
.env
*.key

# model weights (baked into image, not the repo)
models/
*.onnx
*.onnx.data

# eval corpora (downloaded, not vendored)
eval_data/
# eval runs — contain REAL recordings; never publish (esp. on the public repo)
eval_harness/

# deploy secrets (filled locally; pass to phala via -e)
deploy/**/.env.sealed
deploy/**/.deploy.env
deploy/**/*.log

# OS
.DS_Store
